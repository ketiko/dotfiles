#!/bin/bash

if [ ! -d ~/.solarized ]; then
  git clone git://github.com/sigurdga/gnome-terminal-colors-solarized.git ~/.solarized
fi

if which gnome-terminal &> /dev/null; then
  ~/.solarized/set_dark.sh
fi
